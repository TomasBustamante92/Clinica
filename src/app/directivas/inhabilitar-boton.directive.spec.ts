import { InhabilitarBotonDirective } from './inhabilitar-boton.directive';

describe('InhabilitarBotonDirective', () => {
  it('should create an instance', () => {
    const directive = new InhabilitarBotonDirective();
    expect(directive).toBeTruthy();
  });
});
